<h1>All Tags</h1>

<ul id="tags">
  <% @tags.each do |tags| %>
    <li>
      <%= link_to tags.name, tags_path(tag) %>
      <%= link_to "delete", tag_path(tag), method: :delete, data: {confirm: "Really delete the tag?"} %>
    </li>
  <% end %>
</ul>